# 📁 Структура проекта

Детальное описание структуры Cedar Folder Size Analyzer.

## 📂 Корневая структура

```
cedar-folder-size-analyzer/
├── .gitlab/                    # GitLab специфичные файлы
│   ├── issue_templates/        # Шаблоны для issues
│   ├── merge_request_templates/ # Шаблоны для MR
│   ├── api-examples.md         # Примеры GitLab API
│   └── runner-setup.md         # Настройка GitLab Runner
│
├── icon_converter/             # Утилита для конвертации SVG → ICO
│   ├── src/
│   │   └── main.rs
│   └── Cargo.toml
│
├── languages/                  # Файлы переводов (i18n)
│   ├── en.json                 # English
│   ├── ru.json                 # Русский
│   ├── de.json                 # Deutsch
│   ├── zh.json                 # 中文
│   ├── es.json                 # Español
│   └── fr.json                 # Français
│
├── scripts/                    # Вспомогательные скрипты
│   └── create-release.ps1      # Скрипт создания релизов
│
├── src/                        # Исходный код приложения
│   ├── icons/                  # SVG иконки
│   │   ├── cedar.svg           # Логотип приложения
│   │   ├── dark/               # Иконки для тёмной темы
│   │   │   ├── file.svg
│   │   │   ├── folder.svg
│   │   │   ├── search.svg
│   │   │   └── stop.svg
│   │   └── light/              # Иконки для светлой темы
│   │       ├── file.svg
│   │       ├── folder.svg
│   │       ├── search.svg
│   │       └── stop.svg
│   ├── i18n.rs                 # Система интернационализации
│   └── main.rs                 # Главный файл приложения
│
├── target/                     # Скомпилированные артефакты (не в git)
│   ├── debug/                  # Debug сборка
│   ├── release/                # Release сборка
│   │   └── cedar-folder-size-analyzer.exe
│   └── wix/                    # MSI установщики
│       └── *.msi
│
├── wix/                        # Файлы для MSI установщика
│   ├── main.wxs                # WiX конфигурация
│   ├── License.rtf             # Лицензионное соглашение
│   └── Product.ico             # Иконка продукта
│
├── wix-tools/                  # WiX Toolset binaries (не в git)
│   ├── candle.exe              # WiX компилятор
│   ├── light.exe               # WiX линкер
│   └── ...                     # Другие WiX утилиты
│
├── .editorconfig               # EditorConfig настройки
├── .gitattributes              # Git атрибуты файлов
├── .gitignore                  # Игнорируемые файлы
├── .gitlab-ci.yml              # GitLab CI/CD конфигурация
├── build.rs                    # Скрипт сборки (иконка в EXE)
├── Cargo.lock                  # Зависимости (locked)
├── Cargo.toml                  # Конфигурация проекта
├── CHANGELOG.md                # История изменений
├── GITLAB_CI.md                # Документация CI/CD
├── PROJECT_STRUCTURE.md        # Этот файл
├── QUICKSTART.md               # Быстрый старт
└── README.md                   # Основная документация
```

## 📄 Описание ключевых файлов

### Конфигурация проекта

#### `Cargo.toml`
Основной конфигурационный файл Rust проекта:
- Метаданные проекта (имя, версия, автор)
- Зависимости (egui, rayon, sysinfo и т.д.)
- Настройки оптимизации для release сборки

#### `build.rs`
Скрипт сборки, выполняется перед компиляцией:
- Встраивает иконку в Windows EXE файл
- Использует `winresource` для Windows resources

#### `.gitlab-ci.yml`
Конфигурация GitLab CI/CD:
- **Stages:** build, release
- **Jobs:**
  - `build:windows` - Сборка артефактов
  - `lint:check` - Проверка кода
  - `release:gitlab` - Создание релиза

### Исходный код

#### `src/main.rs` (основной файл, ~1600 строк)
Главная логика приложения:

**Структуры данных:**
- `TreeNode` - Узел дерева файловой системы
- `ScanProgress` - Прогресс сканирования
- `AppSettings` - Настройки приложения
- `FolderAnalyzerApp` - Главное приложение

**Основные функции:**
- `scan_directory_parallel()` - Многопоточное сканирование
- `scan_directory_single()` - Однопоточное сканирование
- `render_tree_node()` - Отрисовка дерева
- `format_bytes()` - Форматирование размеров
- Управление темами и языками
- Контекстное меню (удаление, открытие, копирование)

#### `src/i18n.rs`
Система интернационализации:
- Загрузка JSON файлов переводов
- Получение переведённых строк
- Поддержка fallback на английский

### Локализация

#### `languages/*.json`
Файлы переводов в формате JSON:
```json
{
  "key": "Translated text"
}
```

Поддерживаемые языки:
- `en.json` - English (основной)
- `ru.json` - Русский
- `de.json` - Deutsch
- `zh.json` - 中文 (Chinese)
- `es.json` - Español
- `fr.json` - Français

### WiX установщик

#### `wix/main.wxs`
XML конфигурация WiX для создания MSI:
- Метаданные продукта
- Структура установки
- Компоненты (файлы, ярлыки)
- UI диалоги
- Настройки установки/удаления

#### `wix/Product.ico`
Иконка продукта в формате ICO:
- Генерируется из `src/icons/cedar.svg`
- Используется icon_converter

#### `wix/License.rtf`
Лицензионное соглашение в формате RTF:
- Отображается во время установки
- Freeware лицензия

### Скрипты и утилиты

#### `scripts/create-release.ps1`
PowerShell скрипт для автоматизации релизов:
- Обновление версии в Cargo.toml
- Обновление CHANGELOG.md
- Создание git тега
- Отправка в GitLab

**Использование:**
```powershell
.\scripts\create-release.ps1 -Version 0.2.0
```

#### `icon_converter/`
Утилита для конвертации SVG → ICO:
- Читает `src/icons/cedar.svg`
- Генерирует `wix/Product.ico`
- Множественные размеры (16, 32, 48, 256 px)

### Документация

#### `README.md`
Основная документация:
- Обзор проекта
- Возможности
- Инструкции по установке
- Руководство пользователя
- Технические детали

#### `CHANGELOG.md`
История изменений:
- Следует формату [Keep a Changelog](https://keepachangelog.com/)
- Semantic Versioning
- Категории: Added, Changed, Fixed, Removed

#### `GITLAB_CI.md`
Подробная документация CI/CD:
- Как создать релиз
- Структура pipeline
- Требования к runner
- Кэширование
- Troubleshooting

#### `QUICKSTART.md`
Быстрый старт для разработчиков:
- Создание релизов
- Workflow разработки
- Часто используемые команды
- Checklist перед релизом

#### `PROJECT_STRUCTURE.md`
Описание структуры проекта (этот файл).

### GitLab шаблоны

#### `.gitlab/issue_templates/release.md`
Шаблон issue для релиза:
- Checklist подготовки
- Checklist сборки и тестирования
- Checklist публикации

#### `.gitlab/merge_request_templates/default.md`
Шаблон Merge Request:
- Описание изменений
- Тип изменений
- Связанные issues
- Checklist проверки

#### `.gitlab/runner-setup.md`
Инструкция по настройке GitLab Runner:
- Установка на Windows
- Регистрация
- Конфигурация
- Troubleshooting

#### `.gitlab/api-examples.md`
Примеры использования GitLab API:
- Работа с релизами
- Работа с pipelines
- Работа с тегами
- Полезные скрипты

### Конфигурационные файлы

#### `.gitignore`
Игнорируемые файлы git:
- `/target` - Скомпилированные файлы
- `.cargo/`, `.rustup/` - Кэш CI/CD
- `*.msi`, `*.exe` - Бинарные файлы
- IDE файлы

#### `.gitattributes`
Атрибуты файлов для git:
- Окончания строк (LF для исходников, CRLF для Windows файлов)
- Бинарные файлы
- Linguist overrides

#### `.editorconfig`
Настройки редактора:
- Стиль отступов
- Окончания строк
- Кодировка
- Для разных типов файлов

## 🔄 Жизненный цикл разработки

### 1. Разработка

```
Редактирование кода → Компиляция → Тестирование
      ↓                   ↓              ↓
   src/main.rs     cargo build      cargo run
```

### 2. Проверка качества

```
cargo check → cargo clippy → cargo fmt
    ↓              ↓             ↓
 Синтаксис    Предупреждения  Форматирование
```

### 3. Локальная сборка релиза

```
cargo build --release
      ↓
target/release/cedar-folder-size-analyzer.exe
      ↓
WiX candle.exe (компиляция .wxs)
      ↓
WiX light.exe (создание .msi)
      ↓
target/wix/*.msi
```

### 4. CI/CD Pipeline

```
git push origin v0.2.0 (тег)
      ↓
GitLab CI/CD trigger
      ↓
┌─────────────┐
│ build stage │
├─────────────┤
│ build:windows│ → cargo build --release
│             │ → create ZIP
│             │ → create MSI
└─────────────┘
      ↓
┌─────────────┐
│release stage│
├─────────────┤
│release:gitlab│ → create GitLab Release
│             │ → attach artifacts
└─────────────┘
      ↓
Release published!
```

## 📦 Артефакты сборки

После сборки создаются следующие артефакты:

### Debug сборка
```
target/debug/
├── cedar-folder-size-analyzer.exe  (~80-100 MB)
└── cedar_folder_size_analyzer.pdb  (отладочная информация)
```

### Release сборка
```
target/release/
├── cedar-folder-size-analyzer.exe  (~5-8 MB, оптимизировано)
├── cedar_folder_size_analyzer.pdb
└── *.zip                           (архив с EXE)
```

### MSI установщик
```
target/wix/
├── cedar-folder-size-analyzer-<version>-x86_64.msi (~5-8 MB)
├── *.wixobj                        (промежуточные файлы)
└── *.wixpdb                        (отладочная информация WiX)
```

## 🔍 Поиск по проекту

### Найти код для функции
```bash
# Функция сканирования
grep -n "fn scan_directory" src/main.rs

# Обработка контекстного меню
grep -n "context_menu" src/main.rs

# Локализация
grep -n "get_text" src/main.rs
```

### Найти использование зависимости
```bash
# Использование egui
grep -rn "egui::" src/

# Использование rayon (многопоточность)
grep -rn "rayon::" src/
```

## 🎨 Ресурсы

### Иконки
Все иконки в формате SVG находятся в `src/icons/`:

- **Логотип:** `cedar.svg` (дерево Cedar)
- **UI иконки:** две версии (dark/light) для адаптации к теме
  - `folder.svg` - Папка
  - `file.svg` - Файл
  - `search.svg` - Поиск/Сканирование
  - `stop.svg` - Остановка

### Шрифты
Используются системные шрифты через egui:
- Windows: Segoe UI
- Fallback: DejaVu Sans

## 📊 Метрики проекта

- **Строк кода (Rust):** ~1700
- **Зависимости:** ~15 прямых
- **Поддерживаемые языки:** 6
- **Размер EXE (Release):** ~5-8 MB
- **Время сборки (Release):** ~2-3 минуты

## 🔗 Связанные файлы

- [README.md](README.md) - Основная документация
- [GITLAB_CI.md](GITLAB_CI.md) - CI/CD документация
- [QUICKSTART.md](QUICKSTART.md) - Быстрый старт
- [CHANGELOG.md](CHANGELOG.md) - История изменений

