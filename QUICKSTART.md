# 🚀 Быстрый старт: GitHub Actions и релизы

Краткое руководство по использованию GitHub Actions для автоматической сборки релизов.

## ⚡ Быстрое создание релиза

### Вариант 1: Автоматический скрипт (рекомендуется)

```powershell
# Создать релиз версии 0.2.0
.\scripts\create-release.ps1 -Version 0.2.0

# Предварительный просмотр (без отправки)
.\scripts\create-release.ps1 -Version 0.2.0 -DryRun

# С кастомным сообщением
.\scripts\create-release.ps1 -Version 0.2.0 -Message "Added awesome features"
```

Скрипт автоматически:
- ✅ Обновит версию в `Cargo.toml`
- ✅ Обновит `CHANGELOG.md`
- ✅ Создаст коммит
- ✅ Создаст git тег
- ✅ Отправит в GitHub

### Вариант 2: Вручную

```bash
# 1. Обновите версию в Cargo.toml
# version = "0.2.0"

# 2. Обновите CHANGELOG.md

# 3. Создайте коммит
git add Cargo.toml CHANGELOG.md
git commit -m "Release version 0.2.0"

# 4. Создайте тег
git tag -a v0.2.0 -m "Release v0.2.0"

# 5. Отправьте в GitHub
git push origin master
git push origin v0.2.0
```

## 📦 Что произойдёт дальше?

После отправки тега GitHub Actions автоматически:

1. **Соберёт проект** (`cargo build --release`)
2. **Создаст ZIP архив** с исполняемым файлом
3. **Соберёт MSI установщик** через WiX Toolset
4. **Создаст GitHub Release** с описанием
5. **Прикрепит артефакты** к релизу:
   - `cedar-folder-size-analyzer-<version>-x86_64.msi`
   - `cedar-folder-size-analyzer-<version>-x86_64-windows.zip`
   - `cedar-folder-size-analyzer.exe`

## 🔍 Мониторинг сборки

### Проверить статус workflow

1. Перейдите в GitHub: **Actions**
2. Найдите workflow для вашего тега
3. Нажмите на workflow для просмотра деталей

### Проверить релиз

После успешной сборки:
1. Перейдите в **Releases** (в правой панели)
2. Найдите ваш релиз
3. Скачайте артефакты из секции "Assets"

## 🛠️ Настройка (первый раз)

### GitHub Actions уже настроены! ✅

Ничего дополнительного настраивать не нужно - GitHub Actions работают "из коробки".

**Файлы конфигурации:**
- `.github/workflows/release.yml` - Сборка релизов при тегах
- `.github/workflows/build.yml` - Проверка сборки при коммитах

### Проверка прав доступа

В настройках репозитория:

1. **Settings → Actions → General → Workflow permissions**
   - Выберите "Read and write permissions"
   - Включите "Allow GitHub Actions to create and approve pull requests"

2. **Settings → Actions → General**
   - Убедитесь что "Allow all actions and reusable workflows" включено

## 📋 Semantic Versioning

Используйте семантическое версионирование:

| Версия | Когда использовать | Пример |
|--------|-------------------|--------|
| **MAJOR** (1.0.0) | Несовместимые изменения | 0.9.5 → 1.0.0 |
| **MINOR** (0.1.0) | Новые функции (совместимые) | 0.1.5 → 0.2.0 |
| **PATCH** (0.0.1) | Исправления ошибок | 0.1.0 → 0.1.1 |

**Примеры:**
```bash
# Первый релиз
v0.1.0

# Добавлена новая функция
v0.2.0

# Исправлен баг
v0.2.1

# Первая стабильная версия
v1.0.0
```

## 🎯 Workflow разработки

### Обычный цикл разработки

```bash
# 1. Создайте ветку для новой функции
git checkout -b feature/awesome-feature

# 2. Разработайте и протестируйте
# ... код ...

# 3. Закоммитьте изменения
git add .
git commit -m "Add awesome feature"

# 4. Отправьте в GitHub
git push origin feature/awesome-feature

# 5. Создайте Pull Request в GitHub
# GitHub Actions автоматически проверит код

# 6. После одобрения - слейте в master
```

### Создание релиза

```bash
# 1. Убедитесь что вы на master
git checkout master
git pull origin master

# 2. Запустите скрипт создания релиза
.\scripts\create-release.ps1 -Version 0.3.0

# 3. Подтвердите отправку
# GitHub Actions сделает всё остальное!
```

## 🐛 Решение проблем

### Workflow не запускается

**Проверьте:**
- Файл `.github/workflows/release.yml` находится в репозитории
- Тег имеет формат `v*.*.*` (например, `v0.1.0`)
- GitHub Actions включены в Settings → Actions

**Решение:**
```bash
# Проверьте формат тега
git tag -l

# Пересоздайте тег если нужно
git tag -d v0.2.0
git tag -a v0.2.0 -m "Release v0.2.0"
git push origin v0.2.0
```

### Сборка не удаётся

**Проблема:** Workflow завершается с ошибкой

**Решение:**
1. Откройте **Actions** → найдите неудавшийся workflow
2. Кликните на job для просмотра логов
3. Проверьте конкретный шаг с ошибкой

### WiX сборка не удаётся

**Проблема:** MSI не создаётся

**Решение:** Убедитесь что папка `wix-tools/` присутствует в репозитории

### Тег уже существует

**Проблема:** Вы пытаетесь создать уже существующий тег

**Решение:**
```bash
# Удалите тег локально
git tag -d v0.2.0

# Удалите тег на GitHub
git push origin --delete v0.2.0

# Создайте тег заново
git tag -a v0.2.0 -m "Release v0.2.0"
git push origin v0.2.0
```

## 📚 Полезные ссылки

- [.github/workflows/release.yml](.github/workflows/release.yml) - Конфигурация релизов
- [.github/workflows/build.yml](.github/workflows/build.yml) - Конфигурация сборки
- [GITHUB_CI.md](GITHUB_CI.md) - Подробная документация CI/CD
- [CHANGELOG.md](CHANGELOG.md) - История изменений

## 💡 Полезные команды

```bash
# Посмотреть все теги
git tag -l

# Посмотреть последние релизы
git tag -l --sort=-v:refname | head -5

# Посмотреть изменения между версиями
git log v0.1.0..v0.2.0 --oneline

# Посмотреть информацию о теге
git show v0.2.0

# Создать pre-release (alpha/beta/rc)
git tag -a v0.3.0-beta.1 -m "Beta release"
git push origin v0.3.0-beta.1
```

## ✅ Checklist перед релизом

- [ ] Все тесты пройдены
- [ ] Документация обновлена
- [ ] `CHANGELOG.md` актуален
- [ ] Версия в `Cargo.toml` обновлена
- [ ] Код прошёл review
- [ ] Локальная сборка работает: `cargo build --release`
- [ ] MSI установщик протестирован локально

## 🎉 Готово!

Теперь вы можете создавать релизы одной командой:

```powershell
.\scripts\create-release.ps1 -Version 0.2.0
```

GitHub Actions позаботится обо всём остальном! 🚀

---

**Дополнительно:**
- [GitHub Actions Documentation](https://docs.github.com/en/actions)
- [Создание релизов в GitHub](https://docs.github.com/en/repositories/releasing-projects-on-github)
