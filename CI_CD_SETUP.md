# ✅ GitLab CI/CD - Настройка завершена

Этот файл содержит информацию о настроенных компонентах CI/CD для автоматической сборки релизов Cedar Folder Size Analyzer.

## 📋 Созданные файлы

### Основная конфигурация CI/CD

- ✅ **`.gitlab-ci.yml`** - Основная конфигурация GitLab CI/CD
  - Автоматическая сборка при коммитах и тегах
  - Создание MSI установщика
  - Создание ZIP архива
  - Автоматический релиз при тегах
  - Проверка кода (clippy, fmt)

### Документация

- ✅ **`CHANGELOG.md`** - История изменений проекта
- ✅ **`GITLAB_CI.md`** - Подробная документация CI/CD
- ✅ **`QUICKSTART.md`** - Быстрый старт для разработчиков
- ✅ **`PROJECT_STRUCTURE.md`** - Описание структуры проекта
- ✅ **`CI_CD_SETUP.md`** - Этот файл (обзор настройки)

### Скрипты автоматизации

- ✅ **`scripts/create-release.ps1`** - PowerShell скрипт для создания релизов
  - Автоматическое обновление версии
  - Обновление CHANGELOG.md
  - Создание и отправка тегов
  - Режим предпросмотра (DryRun)

### GitLab конфигурация

- ✅ **`.gitlab/issue_templates/release.md`** - Шаблон issue для релиза
- ✅ **`.gitlab/merge_request_templates/default.md`** - Шаблон Merge Request
- ✅ **`.gitlab/runner-setup.md`** - Инструкция по настройке Runner
- ✅ **`.gitlab/api-examples.md`** - Примеры использования GitLab API

### Конфигурационные файлы

- ✅ **`.gitattributes`** - Git атрибуты файлов
  - Корректные окончания строк (LF/CRLF)
  - Определение бинарных файлов
  - Linguist overrides
  
- ✅ **`.editorconfig`** - Настройки редактора
  - Единый стиль кода
  - Отступы и форматирование

### Обновлённые файлы

- ✅ **`README.md`** - Добавлена секция о CI/CD
- ✅ **`.gitignore`** - Добавлены исключения для CI/CD кэша

## 🚀 Как использовать

### Создание релиза (автоматически)

```powershell
# Используйте скрипт для автоматизации
.\scripts\create-release.ps1 -Version 0.2.0

# Скрипт автоматически:
# 1. Обновит версию в Cargo.toml
# 2. Обновит CHANGELOG.md
# 3. Создаст коммит
# 4. Создаст git тег
# 5. Отправит в GitLab
```

### Создание релиза (вручную)

```bash
# 1. Обновите версию в Cargo.toml
# version = "0.2.0"

# 2. Обновите CHANGELOG.md

# 3. Создайте коммит
git add Cargo.toml CHANGELOG.md
git commit -m "Release version 0.2.0"

# 4. Создайте тег
git tag -a v0.2.0 -m "Release v0.2.0"

# 5. Отправьте в GitLab
git push origin master
git push origin v0.2.0
```

### Что произойдёт автоматически

После отправки тега GitLab CI:

1. **Запустит pipeline** с двумя stages:
   - `build` - Сборка артефактов
   - `release` - Создание релиза

2. **Соберёт артефакты:**
   - `cedar-folder-size-analyzer.exe` - Исполняемый файл
   - `cedar-folder-size-analyzer-<version>-x86_64.zip` - ZIP архив
   - `cedar-folder-size-analyzer-<version>-x86_64.msi` - MSI установщик

3. **Создаст GitLab Release:**
   - С описанием из шаблона
   - С прикреплёнными артефактами
   - Доступен в разделе Releases

## ⚙️ Настройка GitLab Runner

### Требования

Для работы CI/CD нужен GitLab Runner на Windows:

- **OS:** Windows 10/11 или Windows Server
- **Тег:** `windows`
- **Executor:** `shell`
- **Shell:** `powershell`

### Быстрая настройка

```powershell
# 1. Скачайте GitLab Runner
Invoke-WebRequest -Uri "https://gitlab-runner-downloads.s3.amazonaws.com/latest/binaries/gitlab-runner-windows-amd64.exe" -OutFile "C:\GitLab-Runner\gitlab-runner.exe"

# 2. Зарегистрируйте runner
cd C:\GitLab-Runner
.\gitlab-runner.exe register
# URL: https://gitlab.com/
# Tag: windows
# Executor: shell
# Shell: powershell

# 3. Установите как службу
.\gitlab-runner.exe install
.\gitlab-runner.exe start
```

Подробная инструкция: [.gitlab/runner-setup.md](.gitlab/runner-setup.md)

## 📦 Структура Pipeline

### Stages

```
┌────────────┐
│   BUILD    │
├────────────┤
│ build:     │ → Сборка Release + ZIP + MSI
│  windows   │   (для master и тегов)
│            │
│ lint:      │ → Проверка кода
│  check     │   (только для веток, не тегов)
└────────────┘
      ↓
┌────────────┐
│  RELEASE   │
├────────────┤
│ release:   │ → Создание GitLab Release
│  gitlab    │   (только для тегов)
└────────────┘
```

### Jobs

#### `build:windows`
- **Когда:** При каждом коммите в master или при создании тега
- **Что делает:**
  1. Устанавливает Rust (если не установлен)
  2. Собирает Release версию: `cargo build --release`
  3. Создаёт ZIP архив
  4. Компилирует WiX файлы: `candle.exe`
  5. Создаёт MSI установщик: `light.exe`
  6. Сохраняет артефакты (срок хранения: 1 неделя)

#### `lint:check` (опционально)
- **Когда:** При коммитах в ветки (не для тегов)
- **Что делает:**
  1. `cargo check` - Проверка компиляции
  2. `cargo clippy` - Статический анализ
  3. `cargo fmt --check` - Проверка форматирования
- **Особенности:** `allow_failure: true` (не блокирует pipeline)

#### `release:gitlab`
- **Когда:** Только при создании тега
- **Что делает:**
  1. Создаёт GitLab Release
  2. Прикрепляет артефакты из `build:windows`
  3. Добавляет описание релиза

### Кэширование

Для ускорения сборки кэшируются:
- `.cargo/` - Cargo registry и git зависимости
- `.rustup/` - Rust toolchain
- `target/` - Скомпилированные зависимости

Кэш создаётся отдельно для каждой ветки.

## 📊 Примеры workflow

### Feature разработка

```bash
# Создать feature ветку
git checkout -b feature/new-awesome-feature

# Разработать и закоммитить
git add .
git commit -m "Add new awesome feature"

# Отправить в GitLab
git push origin feature/new-awesome-feature

# Создать Merge Request
# → CI запустит lint:check

# После одобрения - слить в master
# → CI соберёт артефакты (но не создаст релиз)
```

### Bug fix

```bash
# Создать bugfix ветку
git checkout -b fix/critical-bug

# Исправить и закоммитить
git add .
git commit -m "Fix critical bug in scanner"

# Отправить и создать MR
git push origin fix/critical-bug
```

### Release

```bash
# Вариант 1: Автоматический скрипт
.\scripts\create-release.ps1 -Version 0.2.1

# Вариант 2: Вручную
git tag -a v0.2.1 -m "Release v0.2.1"
git push origin v0.2.1

# → CI соберёт артефакты и создаст релиз
```

## 🔍 Мониторинг

### Проверить статус pipeline

1. GitLab → **CI/CD** → **Pipelines**
2. Найти pipeline для вашего коммита/тега
3. Кликнуть для просмотра детальной информации

### Проверить артефакты

1. Открыть pipeline
2. Кликнуть на job `build:windows`
3. Справа: **Browse** → Просмотр артефактов
4. Или: **Download** → Скачать все артефакты

### Проверить релиз

1. GitLab → **Releases**
2. Найти ваш релиз по тегу
3. Скачать артефакты

## 🐛 Troubleshooting

### Pipeline не запускается

**Проблема:** После push ничего не происходит

**Решение:**
1. Проверьте наличие `.gitlab-ci.yml` в корне репозитория
2. Проверьте GitLab Runner: Settings → CI/CD → Runners
3. Убедитесь что есть активный runner с тегом `windows`

### Сборка не удаётся

**Проблема:** Job `build:windows` падает с ошибкой

**Решение:**
1. Проверьте логи job в GitLab
2. Убедитесь что у runner есть доступ к интернету (для скачивания Rust)
3. Проверьте что WiX tools присутствуют в репозитории

### Релиз не создаётся

**Проблема:** Артефакты собрались, но релиз не создан

**Решение:**
1. Убедитесь что у вас есть Docker runner (тег `docker`)
2. Или закомментируйте `tags: - docker` в job `release:gitlab`
3. Проверьте права доступа: Settings → CI/CD → Token Permissions

## 📚 Документация

### Быстрые ссылки

- 🚀 [QUICKSTART.md](QUICKSTART.md) - Начало работы
- 📖 [GITLAB_CI.md](GITLAB_CI.md) - Детали CI/CD
- ⚙️ [.gitlab/runner-setup.md](.gitlab/runner-setup.md) - Настройка Runner
- 🔧 [.gitlab/api-examples.md](.gitlab/api-examples.md) - GitLab API
- 📁 [PROJECT_STRUCTURE.md](PROJECT_STRUCTURE.md) - Структура проекта

### Внешние ссылки

- [GitLab CI/CD Documentation](https://docs.gitlab.com/ee/ci/)
- [GitLab Runner Documentation](https://docs.gitlab.com/runner/)
- [Rust Documentation](https://doc.rust-lang.org/)
- [WiX Toolset](https://wixtoolset.org/)

## ✅ Checklist завершения

Убедитесь что всё настроено:

### GitLab Repository
- [x] `.gitlab-ci.yml` загружен в репозиторий
- [x] WiX tools присутствуют в репозитории
- [ ] GitLab Runner настроен и активен
- [ ] Runner имеет тег `windows`

### Локальная среда
- [ ] Rust установлен и работает
- [ ] WiX tools работают локально
- [ ] Скрипт `create-release.ps1` протестирован
- [ ] Локальная сборка MSI работает

### Тестирование
- [ ] Тестовый тег создан и отправлен
- [ ] Pipeline запустился успешно
- [ ] Артефакты собрались корректно
- [ ] Релиз создан в GitLab
- [ ] MSI установщик работает

## 🎉 Готово к использованию!

Ваш проект настроен для автоматической сборки релизов!

Просто создайте тег и отправьте его:

```bash
git tag -a v0.1.0 -m "First release"
git push origin v0.1.0
```

GitLab CI сделает всё остальное! 🚀

---

**Дата настройки:** 2025-11-23  
**Версия CI/CD:** 1.0  
**Статус:** ✅ Готово к использованию

