# GitLab CI/CD для Cedar Folder Size Analyzer

## 📋 Обзор

Проект настроен на автоматическую сборку релизов при создании тегов в GitLab.

## 🚀 Как создать релиз

### 1. Подготовка к релизу

Убедитесь, что код готов к релизу:
- Все изменения закоммичены
- Обновлен `CHANGELOG.md` с описанием изменений
- Обновлена версия в `Cargo.toml` (опционально)

### 2. Создание тега

```bash
# Создайте тег с версией (например, v0.1.0)
git tag -a v0.1.0 -m "Release version 0.1.0"

# Отправьте тег в GitLab
git push origin v0.1.0
```

### 3. Автоматическая сборка

После отправки тега GitLab CI автоматически:
1. ✅ Соберёт релизную версию приложения
2. ✅ Создаст ZIP архив с исполняемым файлом
3. ✅ Соберёт MSI установщик через WiX
4. ✅ Создаст GitLab Release с артефактами
5. ✅ Прикрепит все файлы к релизу

### 4. Результат

В разделе **Releases** вашего проекта появится новый релиз с файлами:
- `cedar-folder-size-analyzer-<version>-x86_64.msi` - MSI установщик
- `cedar-folder-size-analyzer-<version>-x86_64.zip` - ZIP архив с EXE
- `cedar-folder-size-analyzer.exe` - исполняемый файл

## 🔧 Структура CI/CD пайплайна

### Stages (Этапы)

1. **build** - Сборка проекта
   - `build:windows` - Сборка релизной версии и артефактов
   - `lint:check` - Проверка кода (только для веток, не для тегов)

2. **release** - Создание релиза
   - `release:gitlab` - Создание GitLab Release (только для тегов)

### Артефакты

Все артефакты автоматически прикрепляются к релизу и доступны для скачивания:
- Исполняемый файл `.exe`
- ZIP архив
- MSI установщик

## 📝 Требования

### GitLab Runner с Windows

Для работы CI/CD требуется GitLab Runner с:
- **Операционная система:** Windows 10/11 или Windows Server
- **Тег:** `windows`
- **PowerShell:** версия 5.1 или выше
- **Интернет доступ** для скачивания Rust toolchain

### Дополнительный Runner (опционально)

Для job `release:gitlab` используется Docker runner:
- **Тег:** `docker`
- **Образ:** `registry.gitlab.com/gitlab-org/release-cli:latest`

Если у вас нет Docker runner, можно закомментировать секцию `tags: - docker` в job `release:gitlab`.

## 🔍 Кэширование

Для ускорения сборки используется кэширование:
- `.cargo/` - Cargo registry и git зависимости
- `.rustup/` - Rust toolchain
- `target/` - Скомпилированные артефакты

Кэш создаётся для каждой ветки отдельно.

## 🛠️ Локальная проверка

Перед отправкой тега можно локально проверить сборку:

```powershell
# Сборка релизной версии
cargo build --release

# Создание MSI
.\wix-tools\candle.exe -nologo -arch x64 -ext WixUIExtension wix\main.wxs `
  "-dCargoTargetBinDir=target\release" `
  "-dVersion=0.1.0" `
  -out target\wix\main.wixobj

.\wix-tools\light.exe -nologo -ext WixUIExtension -ext WixUtilExtension `
  -out target\wix\cedar-folder-size-analyzer-0.1.0-x86_64.msi `
  target\wix\main.wixobj
```

## 🐛 Отладка CI/CD

Если сборка не удалась:

1. Проверьте логи в GitLab CI/CD → Pipelines
2. Убедитесь, что Runner работает и доступен
3. Проверьте, что все файлы WiX присутствуют в репозитории
4. Убедитесь, что версия в теге корректна

## 📚 Полезные команды

```bash
# Посмотреть все теги
git tag -l

# Удалить тег локально
git tag -d v0.1.0

# Удалить тег удалённо
git push origin --delete v0.1.0

# Создать аннотированный тег с подробным описанием
git tag -a v0.1.0 -m "Release v0.1.0

- Feature 1
- Feature 2
- Bug fix 3"

# Отправить все теги
git push origin --tags
```

## 🔄 Semantic Versioning

Рекомендуется использовать [Semantic Versioning](https://semver.org/lang/ru/):

- **MAJOR** (v1.0.0) - Несовместимые изменения API
- **MINOR** (v0.1.0) - Новая функциональность (обратно совместимая)
- **PATCH** (v0.0.1) - Исправления ошибок

Примеры:
- `v0.1.0` - Первый релиз
- `v0.2.0` - Добавлена новая функция
- `v0.2.1` - Исправлена ошибка
- `v1.0.0` - Первая стабильная версия

## ⚙️ Настройка GitLab CI/CD

### Переменные окружения

По умолчанию используются стандартные переменные GitLab:
- `CI_COMMIT_TAG` - Тег коммита
- `CI_COMMIT_SHORT_SHA` - Короткий SHA коммита
- `CI_PROJECT_DIR` - Директория проекта

Дополнительные переменные можно добавить в **Settings → CI/CD → Variables**.

### Права доступа

Убедитесь, что у вашего GitLab runner есть права на:
- Чтение репозитория
- Создание релизов
- Загрузка артефактов

## 📖 Дополнительная информация

- [GitLab CI/CD документация](https://docs.gitlab.com/ee/ci/)
- [GitLab Release CLI](https://gitlab.com/gitlab-org/release-cli)
- [Cargo Book](https://doc.rust-lang.org/cargo/)
- [WiX Toolset](https://wixtoolset.org/)

